{
  "name": "veloop-giveaway",
  "private": true,
  "version": "1.0.0",
  "description": "Root package.json for Vercel's single-project deployment (frontend + API as serverless functions). Not used for local dev — see client/ and server/ for that; run npm install inside each as documented in README.md.",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.2",
    "express-validator": "^7.3.2",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.11.0",
    "mysql2": "^3.23.4",
    "sequelize": "^6.37.8",
    "uuid": "^14.0.2"
  }
}
